# Evidencia adaptador Portal
- **Estado:** Completada
- **Fecha:** 2026-08-08
- **Rama:** `feat/portal-administration-client`

## Contexto
El adaptador debe ejecutar solo publicaciones de servidor autorizadas y documentadas, sin red real ni credenciales.

## Evidencia
RED inicial: `pytest -q tests/test_portal_client.py` falló al importar `transport.fake_portal`. GREEN: el mismo comando produjo `3 passed`; `httpx.MockTransport` verificó POST, path, bearer, cuerpo y una sola llamada ante timeout. La forma `ban` no validada no produjo llamadas HTTP ni de fake; el fake produjo `PublishedMessage(id="fake-1")` offline. `pytest -q` produjo `33 passed` y la advertencia existente de `fastapi.testclient`.

## Riesgos y pendiente
No se usaron llaves ni mutaciones de Portal. Las rutas de acceso no documentadas por MCP siguen rechazadas; las pruebas no sustituyen la provisión operativa futura.

## Archivos relacionados
`backend/tests/test_portal_client.py`, `backend/transport/portal.py`, `backend/transport/fake_portal.py` y `docs/arquitectura/5. Seam de administración Portal.md`.
